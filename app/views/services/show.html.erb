<p style="color: green"><%= notice %></p>

<%= render @service %>

<div>
<% if current_user.admin? %>
  <%= link_to "Edit this service", edit_category_service_path(@category, @service) %> |
  <%= link_to "Destroy this service", category_service_path(@category, @service), method: :delete %>
  <% end %>
  <%= link_to "Back to services", category_path(@category) %>
</div>
