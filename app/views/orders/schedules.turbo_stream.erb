<%= turbo_stream.update "collection-radio-buttons" do %>
  <%= fields_for @order do |order_form| %>
    <%= order_form.collection_radio_buttons :time_slot_id, @time_slots, :id, :time %>
  <% end %>
<% end %>
