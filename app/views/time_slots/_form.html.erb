<%= form_with(model: time_slot, url: url) do |form| %>
  <% if time_slot.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(time_slot.errors.count, "error") %> prohibited this time_slot from being saved:</h2>

      <ul>
        <% time_slot.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :time, style: "display: block" %>
    <%= form.select :time, ["10:00 AM", "11:00 AM", "12:00 PM", "1:00 PM", "2:00 PM", "3:00 PM", "4:00 PM", "5:00 PM"], selected: "10:00 AM" %>
  </div>

  <div>
    <%= form.label :max_slot, style: "display: block" %>
    <%= form.number_field :max_slot %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
